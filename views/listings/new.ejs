<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WanderLust</title>
    <style>
        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(to right, #fdfbfb, #ebedee);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        form {
            background-color: #fff;
            padding: 30px 40px;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            width: 100%;
        }

        h3 {
            font-weight: 700;
            margin-bottom: 25px;
            text-align: center;
            color: #222;
        }

        .form-label {
            display: block;
            margin-bottom: 6px;
            font-weight: 600;
            color: #555;
        }

        .form-control {
            width: 100%;
            padding: 10px 12px;
            margin-bottom: 18px;
            border: 1px solid #ccc;
            border-radius: 6px;
            font-size: 15px;
            box-sizing: border-box;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: #ff7e5f;
            box-shadow: 0 0 8px rgba(255, 126, 95, 0.2);
            outline: none;
        }

        ::placeholder {
            color: #aaa;
            font-style: italic;
        }

        .btn {
            padding: 10px 24px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            width: 100%;
            transition: all 0.3s ease;
        }

        .btn-dark {
            background: linear-gradient(to right, #fe424d);
            color: white;
            
        }

        .btn-dark:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
        }

        /* Flex container for price and location */
        .row {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
        }

        .row>div {
            flex: 1 1 100%;
        }

        /* On wider screens, price and location side by side */
        @media (min-width: 600px) {
            .row>div {
                flex: 1 1 calc(50% - 7.5px);
            }
        }
    </style>
</head>

<body>
    <form method="POST" action="/listings">
        <h3> Create Your Dream Listing </h3>

        <div>
            <label for="title" class="form-label">Title</label>
            <input name="listing[title]" placeholder="E.g., Cozy Mountain Cabin" type="text" class="form-control">
        </div>

        <div>
            <label for="description" class="form-label">Description</label>
            <textarea name="listing[description]" placeholder="Tell visitors what makes it special..." class="form-control"></textarea>
        </div>

        <div>
            <label for="image" class="form-label">Image Link</label>
            <input name="listing[image][url]" placeholder="Paste your image URL here" type="text" class="form-control">
        </div>

        <div class="row">
            <div>
                <label for="price" class="form-label">Price</label>
                <input name="listing[price]" placeholder="Enter nightly rate ($)" type="number" class="form-control">
            </div>

            <div>
                <label for="location" class="form-label">Location</label>
                <input name="listing[location]" placeholder="City or area (e.g., Aspen)" type="text" class="form-control">
            </div>
        </div>

        <div>
            <label for="country" class="form-label">Country</label>
            <input name="listing[country]" placeholder="Where in the world is it?" type="text" class="form-control">
        </div>

        <button class="btn btn-dark">Add Listing</button>
    </form>
</body>

</html>
