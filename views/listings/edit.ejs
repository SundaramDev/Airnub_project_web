<!DOCTYPE html>
<html lang="en">

<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Edit Listing - WanderLust</title>
<style>
    body {
        font-family: 'Plus Jakarta Sans', sans-serif;
        margin: 0;
        padding: 0;
        background: linear-gradient(to right, #fdfbfb, #ebedee);
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
    }
    form {
        background-color: #fff;
        padding: 30px 40px;
        border-radius: 12px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        max-width: 600px;
        width: 100%;
    }
    h3 {
        font-weight: 700;
        margin-bottom: 25px;
        text-align: center;
        color: #222;
    }
    label {
        display: block;
        margin-bottom: 6px;
        font-weight: 600;
        color: #555;
    }
    input, textarea {
        width: 100%;
        padding: 10px 12px;
        margin-bottom: 6px;
        border: 1px solid #ccc;
        border-radius: 6px;
        font-size: 15px;
        box-sizing: border-box;
        transition: all 0.3s ease;
    }
    input:focus, textarea:focus {
        border-color: #ff7e5f;
        box-shadow: 0 0 8px rgba(255, 126, 95, 0.2);
        outline: none;
    }
    ::placeholder {
        color: #aaa;
        font-style: italic;
    }
    button {
        padding: 10px 24px;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-size: 16px;
        font-weight: 600;
        width: 100%;
        background: linear-gradient(to right,  #fe424d);
        color: white;
        transition: all 0.3s ease;
    }
    button:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
    }
    .row {
        display: flex;
        flex-wrap: wrap;
        gap: 15px;
    }
    .row > div {
        flex: 1 1 100%;
    }
    @media (min-width: 600px) {
        .row > div {
            flex: 1 1 calc(50% - 7.5px);
        }
    }

    /* Validation CSS */
    .was-validated input:invalid, .was-validated textarea:invalid {
        border-color: #dc3545;
    }
    .was-validated input:invalid:focus, .was-validated textarea:invalid:focus {
        box-shadow: 0 0 5px rgba(220, 53, 69, 0.5);
    }
    .invalid-feedback {
        display: none;
        color: #dc3545;
        font-size: 0.9rem;
        margin-bottom: 12px;
    }
    .was-validated input:invalid ~ .invalid-feedback,
    .was-validated textarea:invalid ~ .invalid-feedback {
        display: block;
    }
    .valid-feedback {
        display: none;
        color: #28a745;
        font-size: 0.9rem;
        margin-bottom: 12px;
    }
    .was-validated input:valid ~ .valid-feedback,
    .was-validated textarea:valid ~ .valid-feedback {
        display: block;
    }
</style>
</head>

<body>
<form method="POST" action="/listings/<%= listing._id %>?_method=PUT" class="needs-validation" novalidate>
    <h3>Edit Your Listing</h3>

    <div>
        <label for="title">Title</label>
        <input name="listing[title]" value="<%= listing.title %>" type="text" placeholder="E.g., Dreamy Mountain Retreat" required>
        <div class="invalid-feedback">Please enter a valid title!</div>
        <div class="valid-feedback">Title looks good!</div>
    </div>

    <div>
        <label for="description">Description</label>
        <textarea name="listing[description]" rows="4" placeholder="Describe what makes this stay magical..." required><%= listing.description %></textarea>
        <div class="invalid-feedback">Please enter a description!</div>
        <div class="valid-feedback">Description looks good!</div>
    </div>

   <div>
    <label for="image">Image Link</label>
    <input name="listing[image][url]" value="<%= listing.image.url %>" type="text" placeholder="Your stunning property image URL" required>
    <div class="invalid-feedback">Please enter a valid image URL!</div>
    <div class="valid-feedback">Image URL looks good!</div>
</div>


    <div class="row">
        <div>
            <label for="price">Price</label>
            <input name="listing[price]" value="<%= listing.price %>" type="number" min="0" placeholder="Nightly rate ($) – make it enticing!" required>
            <div class="invalid-feedback">Please enter a valid price!</div>
            <div class="valid-feedback">Price looks good!</div>
        </div>

        <div>
            <label for="location">Location</label>
            <input name="listing[location]" value="<%= listing.location %>" type="text" placeholder="City, neighborhood, or landmark (e.g., Aspen)" required>
            <div class="invalid-feedback">Please enter a valid location!</div>
            <div class="valid-feedback">Location looks good!</div>
        </div>
    </div>

    <div>
        <label for="country">Country</label>
        <input name="listing[country]" value="<%= listing.country %>" type="text" placeholder="Where in the world is your place?" required>
        <div class="invalid-feedback">Please enter a valid country!</div>
        <div class="valid-feedback">Country looks good!</div>
    </div>

    <button type="submit">Update Listing</button>
</form>

<script>
(() => {
    'use strict'
    const forms = document.querySelectorAll('.needs-validation')
    Array.from(forms).forEach(form => {
        form.addEventListener('submit', event => {
            if (!form.checkValidity()) {
                event.preventDefault()
                event.stopPropagation()
            }
            form.classList.add('was-validated')
        }, false)
    })
})()
</script>
<!-- <script src="/js/script.js"></script> -->
</body>
</html>
